package org.pokerino.backend.adapter.in;

import org.springframework.stereotype.Controller;

@Controller
public class GameSocketController {

    // Maybe we need to split this into Inbound and Outbound controller
    // -> Outbound controller is for sending messages from the service layer to all clients
    // The new name would be InboundSocketController for this

}
